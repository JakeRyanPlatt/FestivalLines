# Frontend README

This folder holds the React frontend for Festival Lines. If you want the quick version, just install the dependencies and start the dev server.

```bash
npm install
npm run dev
```

